# dataingestion
 to load the chrome_history.csv files from the data directory.
 perform the  join operation on multiple .csv files. so that we can get a final data.
 extract only chrome history title from the files.
 prepare a single text file from the multiple chrome_history.csv files.
 save the final history text file into dataingestion artifact directory.
        